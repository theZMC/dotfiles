#!/usr/bin/env bash

pinentry_programs=(
  pinentry-gnome3
  pinentry-mac
  pinentry-curses
)

for prog in "${pinentry_programs[@]}"; do
  if
    command -v "${prog}" >/dev/null 2>&1
  then
    exec "${prog}" "$@"
  fi
done

echo "Error: No pinentry program found" >&2
exit 1
